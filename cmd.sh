# npm init -y
# mkdir src
# mkdir dist

# install
npm install --save-dev tslint @types/node
npm install --save-dev ts-jest @types/jest

# create ts config (done already)
# npx ts-jest config:init
# tsc --init

npm test